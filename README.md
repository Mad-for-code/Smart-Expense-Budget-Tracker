1. package.json file acts as the master recipe list containing all required libraries (Expo, React Native, React Navigation, AsyncStorage, SVG, Icons, etc.).
2. Just clone or download project from GitHub, and simply open your terminal in the project folder and run:
npm install
